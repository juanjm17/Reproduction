<form method="post" action="{{ path('app_songs_list_delete', {'id': songs_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ songs_list.id) }}">
    <button class="btn">Delete</button>
</form>
