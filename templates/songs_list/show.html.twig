{% extends 'base.html.twig' %}

{% block title %}SongsList{% endblock %}

{% block body %}
    <h1>SongsList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ songs_list.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ songs_list.title }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ songs_list.quantity }}</td>
            </tr>
            <tr>
                <th>Total_duration</th>
                <td>{{ songs_list.totalDuration ? songs_list.totalDuration|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_songs_list_index') }}">back to list</a>

    <a href="{{ path('app_songs_list_edit', {'id': songs_list.id}) }}">edit</a>

    {{ include('songs_list/_delete_form.html.twig') }}
{% endblock %}
